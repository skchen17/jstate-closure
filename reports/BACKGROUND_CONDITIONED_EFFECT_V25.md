# Background-Conditioned Effect — V25

`BACKGROUND_CONDITIONED_CAUSAL_EFFECT = FALSE`. Non-native cosine median is `0.734316`, fidelity-drop median `0.265684`, and conditional norm-ratio standard-deviation median `0.119573`. This panel does not establish background-specific response coordinates.
